apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: mcp-sentinel
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: mcp-sentinel-api
        static_configs:
          - targets: ["mcp-sentinel-api:9090"]
      - job_name: mcp-sentinel-ingest
        static_configs:
          - targets: ["mcp-sentinel-ingest:9091"]
      - job_name: mcp-sentinel-processor
        static_configs:
          - targets: ["mcp-sentinel-processor:9092"]
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: mcp-sentinel
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: mcp-sentinel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: prometheus
          image: prom/prometheus:v2.49.1
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          args:
            - "--config.file=/etc/prometheus/prometheus.yml"
            - "--storage.tsdb.path=/prometheus"
            - "--web.route-prefix=/prometheus"
            - "--web.external-url=/prometheus/"
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus
      volumes:
        - name: config
          configMap:
            name: prometheus-config
