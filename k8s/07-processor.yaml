apiVersion: v1
kind: Service
metadata:
  name: mcp-sentinel-processor
  namespace: mcp-sentinel
spec:
  selector:
    app: mcp-sentinel-processor
  ports:
    - name: metrics
      port: 9102
      targetPort: 9102
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-sentinel-processor
  namespace: mcp-sentinel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-sentinel-processor
  template:
    metadata:
      labels:
        app: mcp-sentinel-processor
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: processor
          image: mcp-sentinel-processor:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 9102
          envFrom:
            - configMapRef:
                name: mcp-sentinel-config
          env:
            - name: METRICS_PORT
              value: "9102"
            - name: OTEL_SERVICE_NAME
              value: mcp-sentinel-processor
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 9102
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 9102
            initialDelaySeconds: 15
            periodSeconds: 20
